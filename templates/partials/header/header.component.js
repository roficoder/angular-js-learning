'use-strict';

angular.module("header").component("headerComponent", {
  templateUrl: "header/header.template.html",
  controller: [
    function headerController($routeParams, $scope, $http) {

    },
  ],
});
